document.addEventListener('DOMContentLoaded',()=>{
  const $=id=>document.getElementById(id);const read=()=>{try{return JSON.parse(localStorage.getItem('medications')||'[]')}catch{return[]}};const now=new Date();
  const profile=JSON.parse(localStorage.getItem('userProfile')||'null')||{};const initials=(profile.name||'U').split(/\s+/).filter(Boolean).slice(0,2).map(x=>x[0]).join('').toUpperCase();$('userInitials').textContent=initials;$('topInitials').textContent=initials;$('sidebarName').textContent=profile.name||'Your account';$('currentDate').textContent=now.toLocaleDateString('en-GB',{weekday:'short',day:'numeric',month:'short'});$('reminderDate').textContent=now.toLocaleDateString('en-GB',{weekday:'long',day:'numeric',month:'long'});
  const active=read().filter(m=>m.status==='active');const doses=active.flatMap(m=>(m.times?.length?m.times:[m.time]).filter(Boolean).map((time,index)=>({time,name:m.name||'Medicine',dosage:m.dosage||'Dose not specified',meal:m.mealTiming||m.notes||'',frequency:m.frequency||'Daily',id:m.id,index}))).sort((a,b)=>a.time.localeCompare(b.time));
  $('reminderCount').textContent=`${doses.length} ${doses.length===1?'reminder':'reminders'}`;const list=$('remindersList');
  if(!doses.length){const empty=document.createElement('div');empty.className='table-empty';empty.innerHTML='<i class="fa-regular fa-bell-slash"></i><strong>No medication reminders yet</strong><p>Add an active medicine with dose times to see reminders here.</p><a class="button secondary" href="medications.html">Add a medicine</a>';list.append(empty);return;}
  doses.forEach((dose,i)=>{const row=document.createElement('article');row.className='full-reminder-item';const time=document.createElement('time');time.textContent=new Date(`2000-01-01T${dose.time}`).toLocaleTimeString('en-GB',{hour:'2-digit',minute:'2-digit'});const icon=document.createElement('span');icon.className='reminder-mark med';icon.innerHTML='<i class="fa-solid fa-pills"></i>';const details=document.createElement('div');details.className='full-reminder-details';const name=document.createElement('strong');name.textContent=`Take ${dose.name}`;const text=document.createElement('p');text.textContent=[dose.dosage,dose.meal].filter(Boolean).join(' · ')||'Medication dose';details.append(name,text);const tag=document.createElement('span');tag.className=`schedule-tag ${i===0?'next-tag':''}`;tag.textContent=i===0?'NEXT DOSE':'SCHEDULED';row.append(time,icon,details,tag);list.append(row);});
});
