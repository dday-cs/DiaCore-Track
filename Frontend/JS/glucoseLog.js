document.addEventListener('DOMContentLoaded',()=>{
  const $=id=>document.getElementById(id);const profile=JSON.parse(localStorage.getItem('userProfile')||'null')||{};const initials=(profile.name||'U').split(/\s+/).filter(Boolean).slice(0,2).map(x=>x[0]).join('').toUpperCase();$('userInitials').textContent=initials;$('topInitials').textContent=initials;$('sidebarName').textContent=profile.name||'Your account';$('currentDate').textContent=new Date().toLocaleDateString('en-GB',{weekday:'short',day:'numeric',month:'short'});$('dateTime').value=new Date(Date.now()-new Date().getTimezoneOffset()*60000).toISOString().slice(0,16);
  $('glucoseForm').addEventListener('submit',e=>{e.preventDefault();const value=Number($('glucoseLevel').value),date=new Date($('dateTime').value);if(!Number.isFinite(value)||value<=0||Number.isNaN(date.getTime()))return;let logs=[];try{logs=JSON.parse(localStorage.getItem('glucoseLogs')||'[]')}catch{}logs.push({date:date.toISOString(),value,context:$('readingContext').value,feeling:$('feeling').value,notes:$('notes').value.trim()});localStorage.setItem('glucoseLogs',JSON.stringify(logs));$('saveMessage').textContent='Reading saved. Your weekly overview is ready.';setTimeout(()=>window.location.href='dashboard.html',550);});
});
